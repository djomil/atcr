<% if notice %>
<aside id="notice"><%= notice %></aside>
<% end %>

<h1>Buy your service</h1>
<ul class="catalog">
    <% @rehab.each do |rehab| %>
<li>
<%= image_tag(rehab.picture_url) %>
<h2><%= rehab.name %></h2>
<p>
<%= sanitize(rehab.service) %>
</p>
<div class="price">
<%= number_to_currency(rehab.price) %>
        <%= button_to 'Add to Cart' %>
</div>
</li>
<% end %>
</ul>

