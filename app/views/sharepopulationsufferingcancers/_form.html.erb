<%= form_with(model: sharepopulationsufferingcancer, local: true) do |form| %>
  <% if sharepopulationsufferingcancer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sharepopulationsufferingcancer.errors.count, "error") %> prohibited this sharepopulationsufferingcancer from being saved:</h2>

      <ul>
      <% sharepopulationsufferingcancer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :entity %>
    <%= form.text_field :entity %>
  </div>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year %>
  </div>

  <div class="field">
    <%= form.label :percentage_population_neoplasms %>
    <%= form.text_field :percentage_population_neoplasms %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
