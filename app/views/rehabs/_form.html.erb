<%= form_with(model: rehab, local: true) do |form| %>
  <% if rehab.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rehab.errors.count, "error") %> prohibited this rehab from being saved:</h2>

      <ul>
      <% rehab.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :service %>
    <%= form.text_area :service %>
  </div>

  <div class="field">
    <%= form.label :picture_url %>
    <%= form.text_field :picture_url %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
