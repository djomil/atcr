<%= form_with(model: orderinshop, local: true) do |form| %>
  <% if orderinshop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(orderinshop.errors.count, "error") %> prohibited this orderinshop from being saved:</h2>

      <ul>
      <% orderinshop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :orderinshop_name, size: 40 %>
   </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address, id: :orderinshop_address, rows: 3, cols: 40 %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.email_field :email, id: :orderinshop_email, size: 40 %>
  </div>

  <div class="field">
    <%= form.label :pay_type %>
    <%= form.select :pay_type, Orderinshop.pay_types.keys, id: :orderinshop_pay_type, prompt: 'Select a payment method' %>
  </div>

  <div class="actions">
    <%= form.submit 'Place Order' %>
  </div>
<% end %>
