require 'rails_helper'

RSpec.describe "Sharepopulationsufferingcancers", type: :request do
  describe "GET /sharepopulationsufferingcancers" do
    it "works! (now write some real specs)" do
      get sharepopulationsufferingcancers_path
      expect(response).to have_http_status(200)
    end
  end
end
